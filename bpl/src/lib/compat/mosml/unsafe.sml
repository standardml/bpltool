(* Glue code to make the ml-lex generated lexer compile with mosml *)

structure Unsafe = struct

    structure Vector = Vector
    structure CharVector = CharVector

end (* structure Unsafe *)